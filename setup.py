from setuptools import setup

setup(
        name='feed-builder',
        version='1.0',
        description='builds my atom feeds',
        author='Billie Enforcie',
        author_email='lefunnypickleman@protonmail.com',
        packages=[],
        install_requires=['bs4'],
        py_modules=['feed-builder'],
        )
